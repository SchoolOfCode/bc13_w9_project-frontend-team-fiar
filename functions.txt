navigation arrows

previousWeek and Next week("button clicked"){
count = current week we can see
previous week button = id -1
next week button = id + 1

if done by date:
date + 7

    const data = await fetch "get post and comments for week(previous/next button id)"
    
    return data
}

different days button (){

}


comments section
- allowing an input field so comment can be added
- submit button which posts data in input field into comment section

morning/afternoon buttons
- switch between boolean
- morning = true
- afternoon = false

different days
- changing between the different days in that specific week
