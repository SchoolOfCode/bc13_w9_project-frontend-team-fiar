import { useState, useReducer, useEffect } from "react";

export function CommentSection() {
  // take in an event (handleChange) - store the value and update list on post section
  // send down as a prop - use to update post
  // take in the change within input bar
  // we want our post comment function to handle the post request
}
// function handleSubmit() {
//   const [text, setText] = useState("");
//   function handleChange(e) {
//     console.log(e.target.value);
//     let input = e.target.value;
//   }

// return (
//   <form onSubmit={handleSubmit}>
//     <input onChange={handleChange} type="text"></input>
//     <button onClick={handleSubmit}> Submit</button>
//     <li></li>
//   </form>
// );
// //}

export default CommentSection;
